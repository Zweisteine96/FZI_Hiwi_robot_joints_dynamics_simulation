<launch>

  <arg name="rvizconfig" default="$(find ar_conveyor_launch)/rviz/config.rviz" />

  <!-- Visualization -->
  <node name="rviz" pkg="rviz" type="rviz" args="-d $(arg rvizconfig)" required="true" />

</launch>

